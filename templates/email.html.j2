<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Minion · Daily Digest</title>

<!-- Force dark scheme -->
<meta name="color-scheme" content="dark only">
<meta name="supported-color-schemes" content="dark">

<style>
:root{
  --accent:#00f0ff;
  --bg:#0b0d10;
  --card:rgba(20,22,26,0.95);
  --text:#e0e5eb;
  --muted:#7a8b9c;
  --glass: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
}
html,body{margin:0;padding:0;background:var(--bg);color:var(--text);font-family:Inter,Segoe UI,system-ui,Arial;}
.container{max-width:760px;margin:36px auto;padding:20px;}
.header{font-size:30px;text-align:center;color:var(--accent);letter-spacing:1px;margin-bottom:18px;text-shadow:0 0 10px rgba(0,240,255,0.12);}
.viewlink{text-align:center;margin-bottom:10px;}
.section{background:var(--card);border-left:4px solid var(--accent);border-radius:14px;padding:18px;margin-bottom:22px;box-shadow:0 6px 24px rgba(0,0,0,0.6);transition:transform .18s ease;}
.section:hover{transform:translateY(-4px);}
.heading{color:var(--accent);font-size:18px;margin-bottom:10px;text-align:center;}
li{margin-bottom:12px;line-height:1.5;}
img.inline{display:block;max-width:100%;border-radius:10px;margin:10px 0;}
.footer{text-align:center;color:var(--muted);font-size:13px;margin-top:24px;}
.math{font-family: "CMU Serif", "Times New Roman", serif;}
.small-muted{color:var(--muted);font-size:12px;margin-top:6px;}
/* fade-in for browser */
@keyframes fadeIn {from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}
.container{animation:fadeIn .6s ease both}
</style>

<!-- MathJax only used for browser-view; email clients ignore script -->
<script>
window.MathJax = {
  tex: { inlineMath: [['$','$'], ['\\(','\\)']] },
  svg: { fontCache: 'global' }
};
</script>
<script async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js"></script>
</head>
<body>
  <div class="container">
    <div class="header">Minion · Daily Digest — {{ date }}</div>

    <div class="viewlink">
      <a href="file://{{ ('' ) }}" style="color:var(--accent);text-decoration:none;">View this digest in your browser (full render)</a>
      <div class="small-muted">Tip: open the saved file <code>data/digest.html</code> if email client strips JS.</div>
    </div>

    {% for section in sections %}
      <div class="section">
        <div class="heading">{{ section.heading }}</div>
        <ul>
          {% for item in section["items"] %}

            <li>
              {% if item.image_url %}
                <img src="{{ item.image_url }}" class="inline" alt="">
              {% endif %}
              <div class="math" style="white-space:pre-wrap;">{{ item.rendered | safe }}</div>
            </li>
          {% endfor %}
        </ul>
      </div>
    {% endfor %}

    <div class="footer">Generated by Minion · minimal cyber-spiritual intelligence</div>
  </div>
</body>
</html>
